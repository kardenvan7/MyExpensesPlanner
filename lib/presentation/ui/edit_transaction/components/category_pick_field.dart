part of '../edit_transaction_screen.dart';

class CategoryPickField extends StatelessWidget {
  const CategoryPickField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(),
    );
  }
}
